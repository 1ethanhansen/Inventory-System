#load text socument with inventory into the program here
entered = str(input("Options: add, change, delete ")).lower()
while (entered != "exit") :
    if (entered == "add") :
        #get more responses to add to the dictionary
    entered = str(input("Options: add, change, delete ")).lower()
